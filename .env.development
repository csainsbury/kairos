# Development Environment Configuration

# Flask settings
FLASK_ENV=development
FLASK_DEBUG=1
SECRET_KEY=dev-secret-key-change-in-production

# LLM API settings (fill in with actual values if available)
LLM_API_KEY=your-actual-api-key-here
LLM_API_URL=https://api.deepseek-api-endpoint/v1/chat/completions

# Todoist API settings (placeholder)
TODOIST_API_KEY=your-todoist-api-key
TODOIST_CLIENT_ID=your-todoist-client-id
TODOIST_CLIENT_SECRET=your-todoist-client-secret
TODOIST_WEBHOOK_URL=http://localhost:5000/todoist/webhook

# Google Calendar API settings (placeholder)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:5000/calendar/oauth2callback

# Database
POSTGRES_USER=kairos_dev
POSTGRES_PASSWORD=kairos_dev_password
POSTGRES_DB=kairos_dev
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db-dev:5432/${POSTGRES_DB}

# Domain Configuration
DEFAULT_DOMAIN_WEIGHTS=work:1.0,life_admin:0.8,general_life:0.6

# Redis cache (if needed)
REDIS_URL=redis://redis:6379/0

# Domain configuration
DOMAIN_NAME=localhost

# Email settings
MAIL_SERVER=localhost
MAIL_PORT=1025
MAIL_USE_TLS=False
MAIL_USE_SSL=False
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_DEFAULT_SENDER=test@example.com